export type Schedule = {
  title: string
  isPrimary: string
  id: string
}

export type Plan = {
  title: string
  id: string
  start: string
  end: string
  description: string
}

export type Friend = {
  name: string
  id: string
}
